using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UISelector : MonoBehaviour
{
    //List<Image> iconSlots = new List<Image>();
    //List<Sprite> icons = new List<Sprite>();
    //RectTransform parentsRectTransform = null;
    //float heightDelta = 0;
    //float widthDelta = 0;

    //int rowCount = 0;
    //int colCount = 0;


    //private void Awake()
    //{
    //    //get anchor points
    //    parentsRectTransform = transform.parent.GetComponent<RectTransform>();

    //    //calculate rows
    //    rowCount = (int) (parentsRectTransform.rect.height / 100.0f);
    //    colCount =(int) (parentsRectTransform.rect.width / 100.0f);
    //    //generate grid
    //    heightDelta = parentsRectTransform.rect.height / rowCount;
    //    heightDelta = parentsRectTransform.rect.width / colCount;

    //    for(int col = 0; col<colCount;col++)
    //    {
    //        for (int row = 0; row < rowCount; row++)
    //        {

    //        }
    //    }
    //}

    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}
}
